/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true, // Feature for highlighting potential problems in an application.
  swcMinify: true, // Function to minify your JavaScript code for production.
  trailingSlash: true, // Function to add a slash "/" after the URL pathname to minimize the potential routing problems
};
